# CI/CD Дамжлагыг Зориудаар Алдаатай Болгох Заавар

GitHub Actions эсвэл Jenkins-ийн дамжлага зөв ажиллаж байгаа эсэхийг шалгахын тулд заримдаа алдааг дуурайх шаардлага гардаг. Дараах гурван аргыг ашиглан алдаа гаргаж болно:

---

## 1-р арга: Угсралтын алдаа (Syntax Error)
Төслийг эмхэтгэх (compile) боломжгүй болгох хамгийн түгээмэл арга.

1. `src/App.tsx` файлыг нээнэ үү.
2. Ямар нэгэн функц эсвэл компонентын гадна талд дурын тэмдэгт нэмж оруулна, жишээ нь:
   ```typescript
   export default App;
   ene_alday_garah_yostoy; // Syntax Error
   ```
3. **Үр дүн**: GitHub Actions/Jenkins дээрх `npm run build` шатлал `TS2304: Cannot find name 'ene_alday_garah_yostoy'` алдаа зааж зогсоно.

---

## 2-р арга: Тестийн алдаа (Зөвлөмж болгож буй арга)
Энэ нь кодыг эвдэхгүйгээр зөвхөн логикийг буруу болгох хамгийн цэвэр арга юм.

1. `src/fail.test.ts` нэртэй файл үүсгэн дараах агуулгыг оруулна:
   ```typescript
   import { expect, test } from 'vitest'

   test('intentional failure', () => {
     expect(1 + 1).toBe(3)
   })
   ```
2. `package.json` файлд `"test": "vitest run"` гэсэн мөрийг нэмнэ.
3. `.github/workflows/deploy.yml` файлд build хийхээс өмнө `npm test` хийх шатлалыг нэмнэ.
4. **Үр дүн**: Дамжлага `Install & Test` шатлал дээр амжилтгүй болж зогсоно.

---

## 3-р арга: Хандах эрх/Тохиргооны алдаа
1. GitHub репозиторийнхоо **Settings > Actions > General** хэсэг рүү орно.
2. **Workflow permissions**-ийг "Read repository contents and package permissions" болгож өөрчилнө (Write эрхийг хасна).
3. **Үр дүн**: `peaceiris/actions-gh-pages` шатлал `403 Permission Denied` алдаа зааж байршуулж чадахгүй.

---

### 🔍 Яагаад үүнийг хийх ёстой вэ?
Дамжлага бүхэлдээ "ногоон" (амжилттай) байх нь сайн хэрэг боловч алдаа гарсан үед амжилттай "улаан" болж зогсдог байх нь таны системийг хамгаалж буй гол үзүүлэлт юм.
